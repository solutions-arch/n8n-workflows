// Feature flags for optional demo tools.
// To enable one: change its value to `true` and push to main — no other changes needed.

// Floating savings calculator (bottom-right corner, every page).
export const CALCULATOR_TOOL_ENABLED = true;
